resource "aws_instance" "my_ec2" {
    ami = "${var.ami_id}" # us-west-2
    instance_type = "${var.instance_type}"
    key_name = "${var.keypair}"
  }
locals {
  id_subnet = "${aws_vpc.test_vpc.id}"
}
